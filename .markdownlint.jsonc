{
  // workflow default markdownlint config.
  // Keep rules enabled; relax only the impractical defaults.
  "default": true,
  "MD013": { "line_length": 120, "code_blocks": false, "tables": false }
}
